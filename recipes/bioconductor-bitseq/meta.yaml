about:
  description: The BitSeq package is targeted for transcript expression analysis and
    differential expression analysis of RNA-seq data in two stage process. In the
    first stage it uses Bayesian inference methodology to infer expression of individual
    transcripts from individual RNA-seq experiments. The second stage of BitSeq embraces
    the differential expression analysis of transcript expression. Providing expression
    estimates from replicates of multiple conditions, Log-Normal model of the estimates
    is used for inferring the condition mean transcript expression and ranking the
    transcripts based on the likelihood of differential expression.
  home: https://bioconductor.org/packages/3.10/bioc/html/BitSeq.html
  license: Artistic-2.0 + file LICENSE
  license_file: LICENSE
  summary: Transcript expression inference and differential expression analysis for
    RNA-seq data
build:
  number: 0
  rpaths:
  - lib/R/lib/
  - lib/
extra:
  identifiers:
  - biotools:bitseq
  parent_recipe:
    name: bioconductor-bitseq
    path: recipes/bioconductor-bitseq
    version: 1.24.0
package:
  name: bioconductor-bitseq
  version: 1.30.0
requirements:
  build:
  - None
  - None
  - automake
  - make
  host:
  - bioconductor-iranges >=2.20.0,<2.21.0
  - bioconductor-rhtslib >=1.18.0,<1.19.0
  - bioconductor-rsamtools >=2.2.0,<2.3.0
  - bioconductor-s4vectors >=0.24.0,<0.25.0
  - r-base
  run:
  - bioconductor-iranges >=2.20.0,<2.21.0
  - bioconductor-rhtslib >=1.18.0,<1.19.0
  - bioconductor-rsamtools >=2.2.0,<2.3.0
  - bioconductor-s4vectors >=0.24.0,<0.25.0
  - r-base
source:
  md5: 2b3839fe7248028b32e554f3f9a57951
  url:
  - https://bioconductor.org/packages/3.10/bioc/src/contrib/BitSeq_1.30.0.tar.gz
  - https://bioarchive.galaxyproject.org/BitSeq_1.30.0.tar.gz
  - https://depot.galaxyproject.org/software/bioconductor-bitseq/bioconductor-bitseq_1.30.0_src_all.tar.gz
test:
  commands:
  - $R -e "library('BitSeq')"
